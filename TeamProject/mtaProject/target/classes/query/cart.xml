<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mta.cart.dao.CartDao">
	
	<insert id = "addCart" parameterType="cart">
	
		insert into m_cart (cart_id, user_id, m_no)
		values(seq_cart.nextval, #{user_id}, #{m_no})
		
	</insert>
	
	<select id="cartList" parameterType="cart" resultType="cart">
		Select 
			c.cart_id,
			c.user_id,
			m.m_title,
			m.user_id,
			m.m_price,
			m.m_coverimage
			
		From
			user_info u, music_board m, m_cart c
		Where 
			u.user_id = c.user_id
			And m.m_no = c.m_no 
			And c.user_id = #{user_id}
			
	
	
	</select>




</mapper>